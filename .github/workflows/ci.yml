name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Verify wheel includes all subpackages
      if: matrix.python-version == '3.12'
      run: |
        pip install build
        python -m build --wheel --outdir dist-wheel
        pip install --force-reinstall dist-wheel/*.whl
        python -c "
        import importlib
        for mod in (
          'sentinel_dv.indexing.store',
          'sentinel_dv.indexing.indexer',
          'sentinel_dv.tools.core',
          'sentinel_dv.schemas.common',
          'sentinel_dv.adapters.cocotb',
        ):
          importlib.import_module(mod)
        print('wheel package check OK')
        "
        pip install -e ".[dev]"

    - name: Run linting (ruff)
      run: |
        ruff check sentinel_dv/ tests/

    - name: Run formatting check (black)
      run: |
        black --check sentinel_dv/ tests/

    - name: Run type checking (mypy)
      run: |
        mypy sentinel_dv/
      continue-on-error: true

    - name: Run tests with coverage
      run: |
        pytest --cov=sentinel_dv --cov-report=xml --cov-report=term --cov-report=json

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        fail_ci_if_error: false
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

    - name: Check coverage threshold
      run: |
        COVERAGE=$(python -c "import json; print(json.load(open('coverage.json'))['totals']['percent_covered'])")
        echo "Coverage: ${COVERAGE}%"
        if (( $(echo "$COVERAGE < 70" | bc -l) )); then
          echo "Coverage ${COVERAGE}% is below 70% threshold"
          exit 1
        fi

    - name: Upload coverage artifacts
      uses: actions/upload-artifact@v4
      with:
        name: coverage-${{ matrix.python-version }}
        path: |
          coverage.json
          coverage.xml
          htmlcov/

  security:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pip-audit safety

    - name: Run pip-audit
      run: |
        pip-audit --desc

    - name: Run safety check
      run: |
        pip install -r requirements.txt
        safety check
      continue-on-error: true
