interface:
  display_name: "Sentinel DV Failure Debugging"
  short_description: "Debug test failures with bounded evidence"
  default_prompt: "Use $sentinel-dv-failure-debugging to investigate why this verification test failed."
dependencies:
  tools:
    - type: "mcp"
      value: "sentinel-dv"
      description: "Read-only Sentinel DV verification intelligence server"
policy:
  allow_implicit_invocation: true
